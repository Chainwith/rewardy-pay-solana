# Optional. The public endpoint works without an API key but may be rate limited.
SOLANA_DEVNET_RPC_URL=https://api.devnet.solana.com

# Optional, devnet only. A JSON array containing a 64-byte throwaway keypair.
# Never use or publish a mainnet key.
SOLANA_DEVNET_PAYER_SECRET=

